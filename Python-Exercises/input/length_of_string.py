# Program that asks the user to enter a string and then prints its length.

string = input("For length of string, Enter string: ")

length_of_string = len(string)

print("The length of string is:", length_of_string)