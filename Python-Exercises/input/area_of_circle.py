# program that takes user input for a radius and calculates the area of a circle.

import math

radius = float(input("Enter the radius of circle: "))

area = math.pi * radius**2

print("The area of circle is:", area)