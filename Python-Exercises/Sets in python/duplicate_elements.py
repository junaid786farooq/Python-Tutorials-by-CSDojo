# program that takes a set as input from the user and removes any duplicate elements, then prints the updated set.

elements = set(input("Enter elements of set separated with spaces: ").split())

my_set = set(elements)

print("Updated Set is:", my_set)