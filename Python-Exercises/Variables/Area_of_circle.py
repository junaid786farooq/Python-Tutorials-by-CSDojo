# Program that calculates the area of a circle using a variable for the radius.

import math

radius = float(input("Enter the radius of the circle: "))

area_of_circle = math.pi * radius ** 2

print("The area if circle is:", area_of_circle)