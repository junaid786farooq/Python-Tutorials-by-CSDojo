# program to find the area of a rectangle using length and width stored in variables.

width = float(input("Enter the width of rectangle: "))
length  = float(input("Enter the length of rectangle: "))

area_of_rectangle = width * length

print("The area of rectangle for given width and height is ", area_of_rectangle)