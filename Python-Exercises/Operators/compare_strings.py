# program that compares two strings entered by the user and checks if they are equal using the equality operator (==).

str1 = input("Enter first string: ")
str2 = input("Enter second string: ")

if str1 == str2:
    print("The strings are equal.")
else:
    print("The strings are NOT equal.")